# Umgebungsvariablen für Essencium Frontend

# Backend API URL
# Standard: http://localhost:8098
# Für Production/Docker: die tatsächliche Backend-URL setzen
VITE_API_URL=http://localhost:8098

# Frontend URL
# Standard: wird automatisch auf window.location.origin gesetzt
# Für Production/Docker: die tatsächliche Frontend-URL setzen
# Diese URL wird für OAuth-Redirects verwendet
VITE_FRONTEND_URL=http://localhost:5173
